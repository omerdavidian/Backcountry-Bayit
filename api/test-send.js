// Test harness disabled — removed to prevent accidental sends.
// If you need to run local dry-runs, recreate a test script outside the
// repository or temporarily enable a local-only script that does not run
// in CI or production environments.